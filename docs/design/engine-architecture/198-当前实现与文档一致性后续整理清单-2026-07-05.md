# 198-当前实现与文档一致性后续整理清单-2026-07-05

## 1. 用途

本文档回答：

```text
197 整理之后，这一轮还剩哪些事需要继续做？
应该按什么顺序一步一步推进？
哪些只是检查，哪些需要重新讨论，哪些需要生成施工文档？
```

本清单不是施工文档。  
真正改功能前，仍必须按 skill 规则生成正式方案 / 施工文档。

## 2. 当前结论

现在不用立刻大重构。

优先做的是：

```text
1. 继续防止 IR 被误用成 runtime 脚本路径。
2. 补齐 AUI ProjectUiStateSnapshot Producer 的方案讨论。
3. 收敛 Rule Authoring 产品化入口，避免用户默认编辑 canonical_ir。
4. 检查 RuntimePackage / Projection 旧名是否继续误导后续实现。
5. 再回到复杂打飞机真实手动编辑 / AI Patch 缺口。
```

## 3. 推荐执行顺序

### Step 1：确认本轮整理是否干净

状态：

```text
已确认，2026-07-05。
```

目标：

```text
确认 197 和代码小修没有引入新的偏差。
```

要做：

```text
读取 195 / 196 / 197。
扫描代码中是否还有公开 IrInterpreter runtime 入口。
扫描文档中是否还有“Canonical Rule IR 是项目规则真相层”等旧说法。
```

建议命令：

```powershell
rg -n "RuleCall::ir_interpreter|pub fn ir_interpreter|pub struct IrInterpreterExecutor|Canonical Rule IR 是项目规则真相层|Canonical Rule IR 是项目逻辑真相" rust/crates 框架设计/引擎总体架构
cargo fmt --check
cargo test -p engine_runtime project_logic
cargo test -p engine_runtime rule_registry
```

验收：

```text
代码中没有公开 IrInterpreter runtime 构造入口。
旧文档命中只允许出现在历史说明或“已修复偏差”上下文。
测试通过。
```

本次确认结果：

```text
公开解释器入口扫描通过：
  未发现 pub fn ir_interpreter / pub struct IrInterpreterExecutor / pub enum RuleExecutorHint / RuleCall::ir_interpreter。

内部 validation-only 占位仍存在：
  engine_runtime::project_logic::RuleCall::validation_only_ir_interpreter
  engine_runtime::logic_executor::IrInterpreterExecutor
  这些均为 pub(crate)，只用于 engine_runtime 内部结构化 unsupported 证据。

旧 IR 真相层说法扫描通过：
  排除 197 / 198 的“已修复偏差 / 检查说明”上下文后，
  未发现 Canonical Rule IR 是项目规则真相层 / 项目逻辑真相层 等当前规则冲突说法。

验证命令通过：
  cargo fmt --check
  cargo test -p engine_runtime project_logic
  cargo test -p engine_runtime rule_registry
```

### Step 2：讨论 AUI ProjectUiStateSnapshot Producer

状态：

```text
方案文档已生成：
199-AUI-ProjectUiStateSnapshot-Producer-v1方案.md
```

目标：

```text
补齐 AUI 的下一块关键缺口：
ProjectUiStateSnapshot 应由谁生成、何时生成、从哪些项目状态读取。
```

当前规则：

```text
AUI Document 只负责 UI 结构。
Binding 只读 ProjectUiStateSnapshot。
复杂 UI 交互逻辑不进 IR。
ProjectUiStateSnapshot Producer 默认属于 Rust Project Framework / Project Rust Module。
```

要讨论的问题：

```text
自走棋 / 打飞机项目中，HUD、装备面板、商店、背包等 UI state 如何聚合？
ProjectUiStateSnapshot 是每帧生成，还是事件驱动缓存？
它从 ECS、project state、runtime service 中读什么？
它如何输出可测试 report？
AI 修改 UI 逻辑时，默认修改 AUI Document、Project Assets 还是 Rust Project Module？
```

产物：

```text
生成正式方案文档：
199-AUI-ProjectUiStateSnapshot-Producer-v1方案.md
```

不直接施工。先讨论并生成方案。

### Step 3：讨论 Rule Authoring 产品化收敛

目标：

```text
把 Rule Authoring 从“用户可能看到 canonical_ir”收敛成“用户看到 Gameplay Rule Asset / RuleSlot / Rule Graph DSL 视图”。
```

当前规则：

```text
ProjectRuleAsset 可以内部保存 canonical_ir。
Canonical Rule IR 是内部规范语义 / 构建输入。
用户和 AI 默认不直接编辑 canonical_ir。
```

要讨论的问题：

```text
RuleSlot 的最小产品形态是什么？
Rule Graph / DSL 视图和 ProjectRuleAsset 如何映射？
AI patch 修改规则时，报告应该指向 RuleSlot 还是 canonical_ir path？
规则错误如何回溯到用户可读的规则资产？
```

产物：

```text
更新或补充 193：
Rule Authoring Productization 当前解释 / 后续方案。
必要时生成 200-RuleSlot-Authoring-Productization-v1方案.md。
```

### Step 4：检查 RuntimePackageBuilder 使用边界

目标：

```text
防止项目目录导出绕过 ProjectRuntimePackageAssembler。
```

当前判断：

```text
测试 fixture 可以直接构造 RuntimePackageBuildInput。
正式 DesktopExportPipeline 已走 ProjectRuntimePackageAssembler。
项目目录导出路径不能绕过 ProjectRuntimePackageAssembler。
```

要检查：

```text
RuntimePackageBuilder::build 是否只在 builder 自测、complex_project_validation、DesktopExportPipeline 中出现。
项目目录导出是否都先经过 ProjectRuntimePackageAssembler。
```

建议命令：

```powershell
rg -n "RuntimePackageBuilder::build|RuntimePackageBuildInput::new|ProjectRuntimePackageAssembler::assemble" rust/crates
```

验收：

```text
正式 export path 仍从 DesktopExportPipeline -> ProjectRuntimePackageAssembler -> RuntimePackageBuilder。
如果发现新路径绕过 assembler，先标注，不直接改大系统。
```

### Step 5：Projection 旧名治理

目标：

```text
减少 render_extract / render_asset_bridge / runtime_scene_hydration 旧文件名对后续 AI 的误导。
```

当前判断：

```text
旧名模块可保留。
本轮已加 module doc，说明它们按 Projection 理解。
暂时不需要为了改名大重构。
```

后续可选动作：

```text
新增 projection facade re-export。
在 110 / 00 / 49 中继续强调旧名映射。
新代码优先使用 Projection 术语命名 report / adapter。
```

是否施工：

```text
P2 可选。
只有当旧名继续导致实现走偏时，再生成施工文档。
```

### Step 6：回到复杂打飞机真实编辑缺口

目标：

```text
继续推进“用户从编辑器手动制作复杂打飞机并导出 Windows 可玩”的真实缺口。
```

当前缺口：

```text
完整用户手动编辑 walkthrough 已能输出 coverage report。
Prefab / Rule / AUI 真实编辑命令仍待补完。
AI Patch 生成闭环仍未完成。
```

下一步候选：

```text
AI Project Patch Entry
或
manual walkthrough coverage report 指出的真实 authoring domain
```

执行规则：

```text
先按新系统讨论规范生成方案。
再根据方案生成施工文档。
施工文档自审后才开始施工。
```

## 4. 优先级

```text
P0：Step 1，确认当前整理干净。
P0：Step 2，讨论 AUI ProjectUiStateSnapshot Producer。
P1：Step 3，讨论 RuleSlot / Rule Authoring 产品化收敛。
P1：Step 4，检查 RuntimePackageBuilder 使用边界。
P2：Step 5，Projection 旧名 facade / 命名治理。
P0/P1：Step 6，回到复杂打飞机真实编辑缺口。
```

如果只选一个下一步：

```text
先做 Step 2：AUI ProjectUiStateSnapshot Producer。
```

原因：

```text
AUI Document / Binding / Present 链路已经存在。
但 ProjectUiStateSnapshot 仍是 package_smoke_snapshot。
不补 Producer，复杂打飞机 / 自走棋的 HUD、装备面板、商店 UI 都只能停留在“有 UI 文件、有绑定占位”，不能进入真实项目状态驱动。
```

## 5. 不要做的事

```text
不要把复杂 UI 交互塞进 IR。
不要重新开放 IrInterpreter 作为 runtime 执行路径。
不要为了旧文件名立刻大规模重命名。
不要绕过 ProjectRuntimePackageAssembler 直接拼项目导出包。
不要把 canonical_ir 作为普通用户默认编辑对象。
```
