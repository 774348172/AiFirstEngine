# 316 持续试玩与画面细节收敛

2026-09-10 用户确认“可以，开始施工下一步”。承接315人工试玩发现：仅两架初始敌机、离屏不清理、无补充；枪口偏移未随缩小素材调整，效果与HUD仍是验证样例。

在项目RuntimeModule内复用现有规则入口和SDK延迟命令，实现定时补充敌机、有限存活数量、越界销毁、玩家活动边界、按住射击与冷却。波次按实际派出的编队推进，敌机计数与可见战区一致。保留两架初始敌机，新增敌机复用同一Prefab及统一尺寸。

表现层：按飞机实际尺寸定位枪口；短时精灵扩张/淡出；两张连续平铺背景滚动；收紧HUD并明确训练模式，不将尚未实现的玩家伤害显示成完整战斗系统。保持资源ID与CC0来源，不新增工具、通用引擎系统或协议。

验收覆盖开局、30/60秒持续运行、开火命中及效果清理，检查实体数量有界和真实画面。完整敌方武器/失败重开、音频、复杂粒子、字体系统和性能资格化不在本轮。

实际集成发现SpriteRenderer2D.color可读不可写，WorldWriteApi只支持visible。为兑现淡出，在既有set_component_field补齐类型正确且有限的RGBA写入，沿既有dirty/RenderProjection同步；不新增API、schema或特效系统。以owner红灯和实际窗口复验作为必要证据。

持续射击验证补充：同帧碰撞结算记录已消耗子弹和待定HP，防止同一目标重复销毁；分数按本帧实际击杀累加。HUD背板不透明，入场/离场敌机不透到文字区域。316已完成，终态见阶段记录。
