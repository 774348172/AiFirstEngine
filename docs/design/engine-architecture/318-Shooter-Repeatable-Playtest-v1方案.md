# 318 Shooter Repeatable Playtest v1

2026-09-10：用户确认接入现有 playtest / observe，将316试玩经验固化为重复验收。

终态：318 A/B完成并归档；13项Rust回归、当前宿主37条断言/3捕获及observe通过。

复用312合同、Compiler与当前宿主9项工具；只修改复杂打飞机项目的观察输出、manifest和测试资产。
项目公开敌机、子弹、特效、分数、波次及项目侧有界/清理事实。使用确定输入持续射击后释放，
在多个模拟检查点验收持续刷敌、连发、击杀计分和最终清理，声明关键呈现截图。
条件采用已有typed等值；复合条件由项目Rust计算，不扩展引擎测试语言。

通过当前宿主构建含317的新Player，执行默认场景，并按返回runRef读取同一运行证据。
保留测试资产与运行说明；不把捕获成功当成美术或性能达标。
不增加工具，不改Provider安装/config，不加入敌机攻击、死亡/重开或性能门槛。

最终范围补充：Provider的既有MCP测试以字符串替换项目observe空实现来注入测试值。
项目实现变化后，同步该测试替换锚点；不改变Provider生产代码。默认窗口场景保留37条断言、
3张截图。数量上限只在声明检查点验收，全程有界由原有项目玩法单测提供补充，不宣称性能资格。
