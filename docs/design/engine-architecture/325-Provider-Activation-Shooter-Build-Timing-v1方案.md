# 325 修复版Provider安装与完整打飞机构建计时

用户授权宿主使用324修复版并测试完整项目。复用324源码回归，编译现有debug Provider，保留旧安装二进制后替换当前配置所指向的可执行文件。配置不变；如宿主无法原地重连，明确区分安装成功与宿主激活待确认，不伪造typed调用。

安装前对候选运行普通MCP initialize/tools/list/inspect及完整项目build两轮（首次/无修改），记录端到端和内部编译耗时、cacheStatus及产物身份。调用真实安装副本再次验证；条件允许在真实宿主typed工具中复测。无新增工具/架构，不改游戏内容，不发布正式release。独立协议探测不得冒称宿主调用。

保留旧二进制哈希和恢复路径；切换失败恢复原路径文件。只结束已确认空闲的本引擎Provider，不关闭Codex或用户游戏窗口。宿主不能重连时，完成其余测试后报告需要重连的事实。约20分钟预算，最多三小时。
